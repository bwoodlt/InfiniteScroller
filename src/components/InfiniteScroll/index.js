import { NAME } from './constants';
import actions from './actions';
import reducer from './reducer';
import InfiniteScroll, { InfiniteScroll as Scroller } from './main';

export default {
    NAME,
    actions,
    reducer,
    InfiniteScroll,
    Scroller,
};
